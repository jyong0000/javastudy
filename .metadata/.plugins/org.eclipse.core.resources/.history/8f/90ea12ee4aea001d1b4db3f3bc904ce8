package project;

class Pizza extends Menu {
    private String size;
    public Pizza(String name, int price, String size) {
        super(name, price);
        this.size = size;
    }
    public String getSize() {
        return size;
    }
    @Override
    public void printInfo() {
        System.out.println(getName() + "(" + size + ") - " + getPrice() + "원");
    }
}